name: Task 0058 Formatting Repair

on:
  push:
    branches:
      - cto/0058-architecture-bottleneck-governance

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cto/0058-architecture-bottleneck-governance
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Format Task 0058 files
        run: |
          pnpm exec prettier --write \
            docs/adr/README.md \
            docs/architecture/bottleneck-governance.json \
            docs/operations/task-0058-architecture-bottleneck-governance.md \
            PRODUCT_ROADMAP.md \
            PROJECT_RULES.md \
            scripts/architecture-bottleneck-governance.mjs \
            scripts/architecture-bottleneck-governance.spec.mjs
          git diff --check
      - name: Commit formatting
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            docs/adr/README.md \
            docs/architecture/bottleneck-governance.json \
            docs/operations/task-0058-architecture-bottleneck-governance.md \
            PRODUCT_ROADMAP.md \
            PROJECT_RULES.md \
            scripts/architecture-bottleneck-governance.mjs \
            scripts/architecture-bottleneck-governance.spec.mjs
          git commit -m "style: format Task 0058 governance files"
          git push origin HEAD:cto/0058-architecture-bottleneck-governance
