export default function DashboardPage() {
  return (
    <section className="mx-auto max-w-[94rem]">
      <p className="text-xs font-bold uppercase tracking-[.2em] text-emerald-700">
        Operations workspace
      </p>
      <h1 className="mt-3 font-[var(--font-display)] text-3xl font-bold tracking-[-.04em] text-[#10271f] sm:text-4xl">
        Dashboard foundation
      </h1>
      <p className="mt-3 max-w-2xl text-sm leading-6 text-[#657770]">
        The authenticated shell is ready. Operational dashboard modules are the next task.
      </p>
    </section>
  );
}
