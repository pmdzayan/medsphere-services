# Local development only.
# Production deployments must explicitly set AUTH_API_URL to the real
# non-loopback auth-service origin; production does not use a localhost fallback.
AUTH_API_URL=http://localhost:3000
